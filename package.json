{
  "name": "pos-print-agent",
  "version": "1.0.0",
  "description": "Local Windows POS print agent exposing a localhost HTTP API for receipt/label printing and cash drawer control.",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsx src/main.ts",
    "build": "tsc && node scripts/copy-setup-ui.js && node scripts/generate-build-info.js",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "package:win": "npm run build && pkg dist/main.js --targets node22-win-x64 --output release/PosPrintAgent/PosPrintAgent.exe",
    "prepare:release": "node scripts/prepare-windows-release.js",
    "release:win": "npm run package:win && npm run prepare:release",
    "zip:release": "node scripts/create-release-zip.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "@yao-pkg/pkg": "^6.21.0",
    "tsx": "^4.16.2",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
